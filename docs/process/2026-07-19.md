# 2026-07-19 进度

当日目标：commit Phase 0-4 成果；Phase 6（login→config 改名 + 表单最大上下文窗口）。

前一日遗留：无未竟事项（Phase 0-4 全部完成，见 2026-07-18.md）。

---

## [00:30] Commit + Phase 6 启动

- Commit `b90ff8b`：Phase 0-4 全部成果（80 files, +2836/-775），未推送（用户未要求）。
  冒烟脚本 `.tools/*.py` 纳入版本管理；`.gitignore` 收窄为仅忽略 `.tools/protoc/`。
- codemap 新增 Phase 6（login→config 改名、表单最大上下文窗口，门禁 G6）。
- 下一步：实现两项改动并过 G6。

---

## [02:30] Phase 6 commit（013cc0d）+ push + Phase 5 启动

- `013cc0d` 已提交并 push 到 origin/main（连同 b90ff8b）。
- 用户决定：**立即执行 Phase 5 全量改名，不要兼容 shim**（不读旧 GROK_* env、不读旧 ~/.grok）。
- 盘点：72 个 xai-* crate（codegen/common/build/prod），非 xai crate 3 个
  （ptyctl/ptyctl-cli/cli-chat-proxy-types）不动；proto 1 个（xai.grok.tools.v1）；
  全仓库 ~1400 文件含 grok/xai 字样。
- codemap Phase 5 已细化为执行计划（门禁 G5）。
- 下一步：阶段A crate/目录/Cargo.toml 改名。

---

## [04:20] Phase 5 完成（硬门禁 G5 通过）✅

- 执行：脚本 `.tools/rename_phase5.py`（保留在仓库内，幂等可复用）——
  92 个路径 git mv（72 crate + grokday/groknight/grok_home/grok-tools.proto 等）、
  1736 个文件内容有序替换（哨兵保护真实 grok.com 域名常量）、Cargo.lock 同步。
- 改名后补扫（机械替换漏掉的形态）：
  - `grokday`/`groknight` 单词标识符（模块/主题别名/tmTheme 资产）
  - `XxxGrok` 后缀（PluginOrigin::ProjectGrok/UserGrok）、`Xai` 前缀（XaiProtoBuilder）、
    `SUPERWEEPCODE`（误伤恢复为 SUPERGROK——xAI 产品名不改）、camelCase xaiAcp*
  - `BuiltinAgentName` 的 strum kebab 派生断裂（WeepCodeBuildPlan→weep-code-build-plan）
    → 全部 CamelCase 标识符统一为 `Weepcode*`（kebab/snake 派生与字符串字面量恢复一致）
  - `parse_and_apply_cwd` 出现 `"weepcode" || "weepcode"` 重复比较（clippy error）→ 合并
  - 加密模板因 Phase 4 改过 prompt.md 需 `scripts/encrypt_templates.py` 重生成
- 验证结果（硬门禁）：
  - G5-1 `cargo check --workspace` 通过（三轮修错）✅
  - G5-2 `git grep -i grok|xai` 命中仅剩白名单：grok.com/x.ai 真实域名常量、SuperGrok、
    历史进度文档、THIRD-PARTY/SOURCE_REV、URL 测试夹具 ✅
  - G5-3 冒烟（~/.weepcode + WEEPCODE_HOME，落盘含 context_window）与 mock 端到端
    （0 xAI 头、模型 id 正确）复跑通过 ✅
  - G5-4 测试：sampler 160、shell auth+provider 482、weeepcode-agent 557、pager 7260 通过
    （仅上游既有 8 个失败，与改名无关，基线已证）；clippy 无新增警告 ✅
- 结论：xai/grok 标识符在全仓库（代码、cargo、配置目录、env 变量、ACP 方法名、文档）
  统一为 weepcode；无兼容 shim，不读旧 GROK_* 与 ~/.grok。
- 下一步：待用户确认提交

---

## [06:10] 全命令审计 + 死命令处置

- 起因：用户发现两个 `/config`（新 provider 表单 vs `/settings` 的 config 别名）。
- 处置：
  - `/settings` 别名去掉 `config`（保留 preferences/prefs），`/config` 归 provider 表单独占；
    脚本核验 82 个 slash 名字+别名无其他冲突
  - CLI 死子命令：`weepcode setup`（xAI 托管配置）、`weepcode update`（xAI 发布通道，
    有覆盖安装上游二进制的风险）→ 打印不可用说明 + exit 1；删除随死的
    run_setup_command/run_update_command/get_channel_switch/signal_leaders_to_relaunch
  - slash 死命令硬隐藏（registry 构造时 fail-closed）：/share /feedback /imagine
    /imagine-video /privacy（xAI 账户/媒体/反馈服务）
  - voice：shell `resolve_voice_mode` 硬关（唯一 STT 后端是 xAI 流式服务）
  - 审计中顺带修复：XaiSessionUpdate 别名统一、SUPERWEEPCODE/SuperWeepcode 两级误伤
    恢复为 SUPERGROK/SuperGrok（xAI 产品 tier 名，bsd sed/perl \b 形态漏网）
  - 测试适配：set_share_visible（share 默认隐藏）、mru 测试改用 plugins、
    apply_auth_meta 的 tier-restricted present 断言区分硬隐藏命令
  - rollback_theme 一次并行失败：隔离单跑通过，确认为并行全局主题缓存干扰（非本次回归）
- 验证：`cargo check --workspace` 通过；pager 全量 7260 通过（仅上游既有 8 失败）；
  `weepcode setup/update` 实测打印不可用并 exit 1；帮助列表无重复 config

---

## [06:45] `/config` → `/add-model`（用户决定）

- 用户指示：保留上游原始 config（`/settings` 的别名），把新增的添加模型命令改名 add-model。
- 完成内容：
  - `/settings` 恢复原始别名 `config`（连同 preferences/prefs）
  - slash：`/add-model`（alias `/login`），文件 add_model.rs（原 config.rs 删除）；
    降级 toast 指向 `/add-model`
  - CLI：`weepcode add-model`（aliases `login`/`config`）打印 Provider 配置指引；
    setup/update 的禁用提示同步指向 add-model
  - 文档：04-slash-commands（/add-model 条目、/logout 文案、/privacy 条目移除）、
    02-authentication、codemap Phase 6 定稿
- 验证：`cargo check --workspace` 通过；相关测试 822 通过；
  `weepcode add-model` 与 `weepcode config`（alias）实测可用；--help 显示 add-model ✅

---

## [01:40] Phase 6 完成（硬门禁 G6 通过）✅

- 完成内容：
  - CLI：`Command::Login` → `Command::Config`（app/cli.rs），clap `alias = "login"` 兼容；
    pager-bin 分发臂同步；打印的配置指引更新（含 context_window 示例）
  - slash：`/config`（`/login` 为 alias），文件 login.rs→config.rs（git mv）；
    会话加载降级 toast 指向 `/config`；user-guide 04-slash-commands 同步
  - 表单第 6 字段「Max context」：预填 200000、纯数字（键入与粘贴均过滤非数字）、
    非法值阻止提交；链路 pager 表单 → Effect::SaveProviderProfile →
    `weepcode/provider/save`（`Option<u64>`）→ `[model.*].context_window`
    （Some 写入 / None 省略由 resolver 默认 200k）；shell 侧零值校验拒绝
- 验证结果（硬门禁）：
  - G6-1 `weepcode config` 与 `weepcode login`（alias）均打印配置指引 ✅；
    `/config` 与 `/login` alias 均注册 ✅
  - G6-2 测试：pager 相关 111、shell provider_profile 10（新增 context_window 往返/
    省略/零值用例）全过 ✅
  - G6-3 PTY 冒烟（更新键序含第 6 字段）：提交后 `config.toml` 含
    `context_window = 128000`，重启免配置 ✅
- 下一步：待用户确认是否就此提交

---
