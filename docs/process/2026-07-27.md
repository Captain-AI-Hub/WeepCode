# 2026-07-27 进度

## 当日目标

- 整理待提交改动，修复提交前发现的编译/格式化阻断，并完成提交推送。

## 进度记录

### 2026-07-27 14:45 — 手动 CI 配置

- 完成内容：
  - 新增 `.github/workflows/manual-ci.yml`，仅支持 `workflow_dispatch` 手动触发，不绑定 `push` / `pull_request`。
  - CI 支持四个平台：Linux x86_64、Linux aarch64、macOS aarch64、Windows x86_64；通过 `platform` 输入选择单个平台，或选择 `all` 跑全部平台。
  - 每个平台执行 protobuf compiler 安装、Cargo 缓存、`cargo fmt --all -- --check`、`cargo check --locked -p weepcode-pager-bin`、`cargo test --locked -p weepcode-pager-render terminal::`、`cargo build --locked --release -p weepcode-pager-bin`，并上传对应二进制 artifact。
  - 同步更新 `docs/project.md` 与 `docs/codemap.md` 的 CI 说明。
- 验证结果：
  - `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/manual-ci.yml"); puts "yaml ok"'` 通过。
  - `git diff --check` 通过。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check --locked -p weepcode-pager-bin` 通过，用时 1m36s。
- 遇到的问题与下一步：
  - 本地只能验证 YAML 与 whitespace；四平台实际构建需推送后在 GitHub Actions 手动触发。

### 2026-07-27 13:05 — 接入上游通用终端修复

- 完成内容：
  - 从上游 `b41c75a` 手工移植 xAI 无关的终端检测修复：新增 herdr multiplexer 检测与 CSI probe gate，保留 tmux/zellij/screen/cmux precedence。
  - 接入带 2 秒边界的 tmux subprocess probe，用于 tmux version、control mode、`extended-keys` 和 option support，避免不健康 tmux server 长时间阻塞启动/诊断。
  - 接入 source-tagged terminal version telemetry：按 brand corroboration 采集 `TERM_PROGRAM_VERSION`、`LC_TERMINAL_VERSION`、`WEZTERM_VERSION`、`VTE_VERSION`，避免把 tmux/zellij 版本误记为终端版本。
  - 给 `/gboom` 模块补充非生产代码说明。
- 验证结果：
  - `cargo fmt` 通过。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check -p weepcode-telemetry` 通过，用时 1m38s。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check -p weepcode-pager-render` 通过，用时 2m51s。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check -p weepcode-pager` 通过，用时 6m37s。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check -p weepcode-pager-bin` 通过，用时 2m28s。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo test -p weepcode-pager-render terminal::` 通过，229 passed，0 failed。
- 遇到的问题与下一步：
  - 上游 near-deadline tmux 测试在当前机器负载下 1.5s 时间窗不稳定，已改成验证“leader 退出但 descendant 持有 pipe 时仍能通过进程组 teardown drain 输出”的稳定测试。
  - 编译耗时主要来自 pager/bin 依赖链较宽：即使只检查 pager-render，也会拉到 agent/workspace/tools/telemetry 等 codegen crates；并行 cargo 任务还会争用同一个 build directory lock。

### 2026-07-27 12:08 — 上游 fork 改动审查

- 完成内容：
  - 确认 `gh` 可用：当前账号 `jelasin` 已登录 GitHub，具备 `repo`/`workflow` scope。
  - 确认 fork parent：`Captain-AI-Hub/WeepCode` fork 自 `xai-org/grok-build`，双方默认分支均为 `main`。
  - 拉取上游到本地 `upstream/main`，审查分叉关系与文件级改动范围。
- 验证结果：
  - `gh api repos/xai-org/grok-build/compare/Captain-AI-Hub:main...main`：上游相对本仓多 9 个 sync commit，本仓相对上游多 3 个 WeepCode 改造 commit，状态 `diverged`。
  - `git diff --stat main...upstream/main`：上游侧差异覆盖 1098 个文件，约 170078 insertions / 62488 deletions，主要集中在 pager/shell/tools/workspace，并包含本仓已删除的 voice/update/billing/privacy/feedback 等 Grok/xAI 专属路径。
- 遇到的问题与下一步：
  - 决策：不建议直接 merge/rebase 上游；会高概率回灌已删除的登录墙、billing、voice、update、feedback/privacy 等子系统，且与 Phase 5/7 改名和死代码切除冲突。
  - 建议后续只按主题 cherry-pick/手工移植：安全修复（bash permission/SSRF/workspace confinement）、启动性能（后台模型/settings fetch）、会话内存/存储修复、终端检测等。

### 2026-07-27 11:16 — Phase 7 死代码清理

- 完成内容：
  - 删除已从 workspace 摘除但仍残留在文件系统/索引中的 `crates/codegen/weepcode-update` 与 `crates/codegen/weepcode-mixpanel`。
  - 清理 Mixpanel 惰性配置字段：移除 `TelemetryConfig::{mixpanel_token,mixpanel_enabled}`、对应 env/build-env 覆盖、requirements 强制项与测试断言。
  - 清理 subscription tier/upsell 残留链路：删除 shell/pager settings 与 auth meta 的 `subscription_tier(_display)` 透传，删除 `tier.rs` 与 JWT tier fallback，移除 image/video 工具本地 `tier_restricted` 短路和 SuperGrok 文案夹具。
  - 保留 announcements 子系统：`2026-07-22` 进度已记录该项需单独决策，本轮不越界删除。
- 验证结果：
  - `cargo fmt` 通过。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check --workspace` 通过；最新一次在文档/测试夹具继续清理后复跑通过，用时 6m08s。
  - `cargo test -p weepcode-shell sampling::error` 启动后长时间未退出，已中断（退出码 130），未取得测试结果。
- 遇到的问题与下一步：
  - `git rm` 首次因文件已 staged 被拒绝；确认无 Cargo 引用后用 `git rm -rf` 删除残留 dead crate。
  - 下一步：如需更强门禁，可单独留时间跑受影响 crate 的测试；本轮以 workspace 编译门禁为准。

### 2026-07-27 00:00 — Phase 7 门禁修复

- 完成内容：
  - 恢复仍被会话/仪表盘公告链路引用的 `views/announcements.rs`、`slash/commands/announcements.rs`、`util/config/announcements.rs`。
  - `weepcode-shell` 重新声明 `weepcode-announcements` workspace 依赖。
  - 欢迎页渲染对齐当前 `hero_box`：移除已删除 welcome announcement helper 的调用与返回字段读取。
  - 清理已删除 campaign/announcement PTY 文件在 `pty_e2e_config_ui.rs`、`leader_pty_e2e/mod.rs` 中的残留 `mod` 声明。
- 验证结果：
  - `cargo fmt` 通过。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check --workspace` 通过，0 error。
- 遇到的问题与下一步：
  - 首次 `cargo check --workspace` 发现 announcements 模块半删除导致 `weepcode-pager` 编译失败；已修复。
  - 下一步：提交并推送当前工作树。
