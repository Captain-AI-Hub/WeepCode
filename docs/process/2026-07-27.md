# 2026-07-27 进度

## 当日目标

- 整理待提交改动，修复提交前发现的编译/格式化阻断，并完成提交推送。

## 进度记录

### 2026-07-27 11:16 — Phase 7 死代码清理

- 完成内容：
  - 删除已从 workspace 摘除但仍残留在文件系统/索引中的 `crates/codegen/weepcode-update` 与 `crates/codegen/weepcode-mixpanel`。
  - 清理 Mixpanel 惰性配置字段：移除 `TelemetryConfig::{mixpanel_token,mixpanel_enabled}`、对应 env/build-env 覆盖、requirements 强制项与测试断言。
  - 清理 subscription tier/upsell 残留链路：删除 shell/pager settings 与 auth meta 的 `subscription_tier(_display)` 透传，删除 `tier.rs` 与 JWT tier fallback，移除 image/video 工具本地 `tier_restricted` 短路和 SuperGrok 文案夹具。
  - 保留 announcements 子系统：`2026-07-22` 进度已记录该项需单独决策，本轮不越界删除。
- 验证结果：
  - `cargo fmt` 通过。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check --workspace` 通过；最新一次在文档/测试夹具继续清理后复跑通过，用时 6m08s。
  - `cargo test -p weepcode-shell sampling::error` 启动后长时间未退出，已中断（退出码 130），未取得测试结果。
- 遇到的问题与下一步：
  - `git rm` 首次因文件已 staged 被拒绝；确认无 Cargo 引用后用 `git rm -rf` 删除残留 dead crate。
  - 下一步：如需更强门禁，可单独留时间跑受影响 crate 的测试；本轮以 workspace 编译门禁为准。

### 2026-07-27 00:00 — Phase 7 门禁修复

- 完成内容：
  - 恢复仍被会话/仪表盘公告链路引用的 `views/announcements.rs`、`slash/commands/announcements.rs`、`util/config/announcements.rs`。
  - `weepcode-shell` 重新声明 `weepcode-announcements` workspace 依赖。
  - 欢迎页渲染对齐当前 `hero_box`：移除已删除 welcome announcement helper 的调用与返回字段读取。
  - 清理已删除 campaign/announcement PTY 文件在 `pty_e2e_config_ui.rs`、`leader_pty_e2e/mod.rs` 中的残留 `mod` 声明。
- 验证结果：
  - `cargo fmt` 通过。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check --workspace` 通过，0 error。
- 遇到的问题与下一步：
  - 首次 `cargo check --workspace` 发现 announcements 模块半删除导致 `weepcode-pager` 编译失败；已修复。
  - 下一步：提交并推送当前工作树。
