# 2026-07-28 进度

## 当日目标

- 继续 2026-07-27 遗留的 deep-research 完整移植：补齐文档、格式化和最终门禁。
- 修复裸 `cargo build` 的 protoc 自动发现，并把手动 CI 产物升级为可一键安装的四平台发布包。
- 修复 Responses Provider 的非标准 `keepalive` SSE 导致 workflow 子 Agent 反序列化失败。

## 进度记录

### 2026-07-28 21:22 — Windows CI 条件编译修复

- 完成内容：
  - 定位 `weepcode-pager-render` Windows `lib test` 失败是非 Windows 的 `build_open_path_command` 测试缺少同等 `cfg` 边界，`E0282` 是函数缺失后的连带错误。
  - 将该测试限定为非 Windows；同时收紧 terminal probe 的 Unix-only `Duration` import。
  - 收紧 `weepcode-shared` Linux-only 进程 import，并改写 arboard 错误保留逻辑，消除 Windows 下未使用 import/初始赋值 warning。
- 验证结果：
  - `env -u PROTOC cargo test -p weepcode-pager-render terminal::` 通过，229 passed，761 filtered out。
  - `env -u PROTOC cargo check -p weepcode-shared --tests` 通过，无 warning。
  - `cargo fmt --all -- --check` 与 `git diff --check` 通过。
- 遇到的问题与下一步：
  - 当前主机未安装 Windows Rust target，需继续用本机门禁验证共享 crate、格式与 diff；最终 Windows 平台编译由 CI runner 复验。

### 2026-07-28 18:52 — Responses 子 Agent keepalive 兼容

- 完成内容：
  - 定位报错不在 deep-research Rhai 脚本，而在所有 child `SessionActor` 共用的 Responses SSE decoder。
  - 在 `weepcode-sampler/src/client.rs` 的强类型反序列化前拦截兼容 Provider 心跳：SSE event 名或 JSON 顶层 `type` 精确等于 `keepalive` 时跳过。
  - 判定保持严格：普通输出文本包含 `keepalive`、其他未知 event 和非 JSON 数据不会被吞掉。
  - 新增 2 个单元回归测试，覆盖 event-name/payload-type 心跳以及正常内容/未知事件保留。
  - 新增 actor 线级回归测试，在真实 Responses SSE 序列中插入 `{"type":"keepalive"}`，验证请求不重试且答案正常完成。
- 验证结果：
  - `env -u PROTOC cargo test -p weepcode-sampler responses_keepalive_detection` 通过，2 passed，160 filtered out。
  - `env -u PROTOC cargo test -p weepcode-sampler --test test_actor responses_keepalive_is_ignored_without_retrying` 通过，1 passed，14 filtered out。
  - `env -u PROTOC cargo check -p weepcode-sampler` 通过，用时 50.45s。
  - `cargo fmt --all -- --check` 与 `git diff --check` 通过。
- 遇到的问题与下一步：
  - 首次格式门禁仅发现测试断言换行差异，执行 `cargo fmt --all` 后通过。
  - actor 测试首次受沙箱限制无法绑定 `127.0.0.1` 临时端口；在允许本地 mock server 监听后通过。
  - 下一步复跑实际 deep-research/sub-agent 请求，确认 Provider 的 `keepalive` 被透明忽略。

### 2026-07-28 17:49 — 四平台安装包与一键安装链路

- 完成内容：
  - 将手动 CI 改为动态四平台矩阵，以 `release-dist` profile 构建并生成 Linux/macOS `tar.gz`、Windows `zip` 安装包。
  - 新增发布输入校验：只有 `platform=all` 才可发布；发布任务创建或更新 latest GitHub Release，并附安装包、两套安装器与 `SHA256SUMS`。
  - 新增 `install.sh` 与 `install.ps1`：自动识别受支持平台、下载 latest 或指定 tag、验证 SHA-256、安装到用户目录；Windows 同步更新用户 `PATH`。
  - README、`docs/project.md` 与 `docs/codemap.md` 同步安装命令、产物命名和发布流程。
- 验证结果：
  - `sh -n install.sh` 通过。
  - Ruby `YAML.load_file(".github/workflows/manual-ci.yml")` 通过，workflow YAML 语法可解析。
  - 提取并实际执行 workflow 的动态矩阵脚本，选择 `macos-aarch64` 时仅输出对应 runner、二进制路径与安装包名，断言通过。
  - 离线构造 `weepcode-macos-aarch64.tar.gz` 与 `SHA256SUMS`，以 mock 下载运行完整 `install.sh`：
    SHA-256 校验、解压、0755 落盘和二进制逐字节比对均通过；包内含二进制、README、许可证与第三方声明。
  - 使用内容不同但文件名相同的包复跑安装器，安装器以 checksum mismatch 退出且未写入目标二进制。
  - `env -u PROTOC cargo test -p weepcode-proto-build` 通过，3 passed。
  - `env -u PROTOC cargo check --locked -p weepcode-pager-bin` 通过，用时 2m01s。
  - `cargo fmt --all -- --check`、`bash -n install.sh` 与 `git diff --check` 通过。
- 遇到的问题与下一步：
  - GitHub Actions 的 job-level `if` 早于 matrix 展开，不能用 `matrix.platform` 过滤；已改为验证任务输出动态 matrix，避免启动未选择的平台 runner。
  - 本机未安装 `pwsh`，PowerShell 安装器的真实下载/解包尚未本地执行；CI 已增加 Windows PowerShell parser 门禁，另已完成人工静态复核。

### 2026-07-28 17:12 — 裸 cargo build 自动发现本地 protoc

- 完成内容：
  - `weepcode-proto-build` 的发现顺序新增仓库 `.tools/protoc/bin/protoc`，优先于依赖 DotSlash 的 `bin/protoc`。
  - 将候选执行失败提示改为包含实际路径的通用错误，不再一律误报缺少 dotslash。
  - README 与 `AGENTS.md` 同步：`PROTOC` 仍可覆盖，但本地 `.tools` 路径会自动发现。
- 验证结果：
  - 当前环境未设置 `PROTOC`，且 `PATH` 中无系统 `protoc`；`.tools/protoc/bin/protoc --version` 输出 `libprotoc 29.3`。
  - `env -u PROTOC cargo build` 成功越过原失败的 `weepcode-tools-api` codegen；完整构建由用户确认通过。
  - `cargo fmt --all -- --check` 与 `git diff --check` 通过。
- 遇到的问题与下一步：
  - 无新增问题；继续完善安装包 CI。

### 2026-07-28 15:52 — 裸 cargo run TUI 冒烟

- 完成内容：
  - 按用户实际路径执行裸 `cargo run`，等待构建完成并启动 `target/debug/weepcode`。
  - 在真实 TUI 输入 `/deep`，检查 slash autocomplete 渲染。
- 验证结果：
  - `cargo run` 构建并启动成功。
  - TUI 下拉明确显示 `/deep-research`，描述为 `Research with bounded parallel agents...`。
  - 测试 TUI 进程已终止，无遗留 WeepCode 测试进程。
- 遇到的问题与下一步：
  - 根因最终确认是默认 `weepcode-build` toolset 漏接 `WorkflowTool`；仅修 bootstrap 广告不足以抵抗 live command update 覆盖。
  - 下一步执行最终 diff/status 检查；改动暂不提交。

### 2026-07-28 15:58 — 全量 weepcode-build toolset 补齐 WorkflowTool

- 完成内容：
  - 根据用户 session 就绪后 `/deep` 仍无提示的截图，确认之前的冒烟只覆盖了 `Starting session…` bootstrap 阶段。
  - 当前模型 `gpt-5.6-sol` 使用 `weepcode-build-plan` 专用 toolset；该 toolset 以及 concise、hashline、orchestrator、plan-no-subagents、ask-user 均漏接上游已有的 `WorkflowTool`。
  - 将 `WorkflowTool` 补入上游对应的全部 7 个 `weepcode-build*` toolset（含此前已补的 default）。
  - 新增逐 toolset 回归测试，防止切换模型/agent type 后 live command update 再次删除 workflow slash 命令。
- 验证结果：
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo test -p weepcode-agent weepcode_build_toolsets_include_workflow_tool` 通过，1 passed。
- 遇到的问题与下一步：
  - 前两次冒烟在 session 完全就绪前输入命令，只验证到 bootstrap 命令目录，未覆盖 live `AvailableCommandsUpdate` 覆盖。
  - 下一步等待 session 完全就绪后再执行裸 `cargo run` TUI 冒烟。

### 2026-07-28 15:34 — 默认 toolset 补入 WorkflowTool

- 完成内容：
  - 复现“新编译仍无 `/deep-research`”后继续检查 live session tool catalog。
  - 定位完整根因：`WorkflowTool` 已注册到全局 tool registry，但本地 `default_weepcode_build_toolset()` 漏了上游已有的 `WorkflowTool` 条目。
  - 将 `WeepcodeBuild:workflow` 加入默认工具集；否则 session 创建后的 `AvailableCommandsUpdate` 会判断 workflow 不可用并覆盖 bootstrap 命令。
  - 扩充默认工具集测试，显式断言 `WeepcodeBuild:workflow` 存在。
- 验证结果：
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo test -p weepcode-agent test_default_tool_config_has_weepcode_build_tools` 通过，1 passed。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo build -p weepcode-pager-bin` 通过，用时 5m07s。
  - 向 `target/debug/weepcode agent --no-leader stdio` 发送真实 ACP initialize 请求，返回的 `meta.availableCommands` 同时包含 `deep-research` 与 `workflow`。
  - 实际启动 `target/debug/weepcode --no-leader` TUI，输入 `/dee`，下拉显示 `/deep-research` 及其描述。
- 遇到的问题与下一步：
  - 前一轮只修了 initialize/bootstrap 广告，没有发现 live session update 会因默认 toolset 漏项再次删除命令。
  - 真实 TUI 冒烟已通过；下一步仅剩最终格式与 diff 检查。

### 2026-07-28 15:18 — Deep Research slash 提示最终门禁

- 完成内容：
  - 完成 initialize metadata、shell gate 与 pager bootstrap autocomplete 的完整修复。
  - 同步更新 `docs/deep-research.md`、`docs/project.md` 和 `docs/codemap.md`。
- 验证结果：
  - `cargo fmt --all -- --check` 通过。
  - `git diff --check` 通过。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check -p weepcode-shell -p weepcode-pager-bin` 通过，用时 1m46s。
- 遇到的问题与下一步：
  - 无新增问题。
  - 用户重新执行 `cargo run -p weepcode-pager-bin` 后，输入 `/deep-` 应出现 `/deep-research` 提示。

### 2026-07-28 15:08 — Deep Research 首次 slash 提示修复

- 完成内容：
  - 定位 `/deep-` 无提示根因：初始化阶段 workflow gate fail-closed，且 `/workflow` 被错误地整体绑定到“已有 workflow run”条件。
  - 新增 `WorkflowLaunchOrManagement` gate，使 `/workflow` 在可启动 workflow 或已有 run 可管理时均可见。
  - WeepCode 顶层 workflow runtime 当前固定启用，因此初始化 metadata 直接广告 `/deep-research` 与 `/workflow`。
  - pager 补充 `/deep-` 匹配 `/deep-research` 的 bootstrap autocomplete 回归测试。
- 验证结果：
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo test -p weepcode-shell --lib workflow_launch_capability_advertises_first_run_commands` 通过，1 passed。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo test -p weepcode-shell --lib existing_run_keeps_management_command_without_launch_capability` 通过，1 passed。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo test -p weepcode-pager --lib deep_research_bootstrap_command_matches_typed_prefix` 通过，1 passed。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo test -p weepcode-shell --lib pre_session_commands_include_workflows_but_not_runtime_tool_gates` 通过，1 passed。
  - `rg -n "pre_session_command_availability|WorkflowLaunchOrManagement|/deep-|initialize metadata|首次启动 autocomplete" ...` 命中 shell、pager 测试与三份同步文档。
- 遇到的问题与下一步：
  - 最新上游也只在存在 `resolve_workflows()` 配置 gate 时预广告；本地没有该 gate，且 runtime 固定开启，因此不能照搬其 fail-closed 默认。
  - 下一步跑最终格式、diff 和主二进制编译门禁。

### 2026-07-28 14:28 — 最终门禁复验

- 完成内容：
  - 在 pager 测试 fixture 修复后，复跑最终格式、whitespace 和生产编译门禁。
- 验证结果：
  - `cargo fmt --all -- --check` 通过。
  - `git diff --check` 通过。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check -p weepcode-tools -p weepcode-shell -p weepcode-pager -p weepcode-pager-bin` 通过，用时 3m20s。
  - 追加本条进度后再次 `git diff --check` 通过。
- 遇到的问题与下一步：
  - 无新增问题。
  - 本轮 deep-research 完整移植收尾进入工作区检查。

### 2026-07-28 14:24 — Pager workflow UI 测试复验

- 完成内容：
  - 补齐 pager slash command 测试 fixture 的 `workflows_available` 字段。
  - 删除不存在的 acp_handler announcement 测试模块声明及无引用 helper。
  - 修复 pager 测试中的 `String + &String` 编译问题。
  - 为 leader PTY e2e common re-export 添加局部 `allow(unused_imports)`，消除 filtered `--tests` warning。
- 验证结果：
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check -p weepcode-pager --tests` 通过，用时 1m47s，无 warning。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo test -p weepcode-pager --lib workflow` 通过，42 passed。
- 遇到的问题与下一步：
  - 首次 `cargo test -p weepcode-pager workflow` 暴露旧测试 fixture 漏字段、已删除 billing 字段残留、缺失 test module 和两处 Rust 2024 下的 `String + &String` 问题，均已修复。
  - 下一步跑最终 fmt/diff 和生产编译复验。

### 2026-07-28 13:45 — Workflow targeted tests 复验

- 完成内容：
  - 复验内置 `deep-research` 脚本关键路径、workflow 保存路径 canonical 化，以及 `weepcode-workflow` 引擎测试。
- 验证结果：
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo test -p weepcode-shell deep_research_binds_shards_and_renders_verified_claims` 通过，目标测试 1 passed。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo test -p weepcode-shell --lib save_through_symlinked_session_root_stays_in_canonical_project` 通过，目标测试 1 passed。
  - `cargo test -p weepcode-workflow` 通过，59 passed，doc-tests 0 passed。
- 遇到的问题与下一步：
  - 第一条 shell targeted test 未加 `--lib`，Cargo 扫描了全部集成测试二进制过滤流程，耗时较长但通过。
  - 下一步跑 pager workflow UI targeted tests。

### 2026-07-28 13:04 — 生产编译链路复验

- 完成内容：
  - 对最新 deep-research/workflow、subagent 深度配置和 pager 接线改动执行生产编译检查。
- 验证结果：
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check -p weepcode-tools -p weepcode-shell -p weepcode-pager -p weepcode-pager-bin` 通过，用时 2m56s。
- 遇到的问题与下一步：
  - 无新增编译问题。
  - 下一步跑 workflow 相关 targeted tests。

### 2026-07-28 12:43 — 格式化与 diff whitespace 复验

- 完成内容：
  - 对最新 deep-research/subagent 深度配置补丁和文档改动执行最终格式化检查。
- 验证结果：
  - `cargo fmt --all -- --check` 通过。
  - `git diff --check` 通过。
- 遇到的问题与下一步：
  - 无新增问题。
  - 下一步跑 tools/shell/pager/pager-bin 编译与 workflow targeted tests。

### 2026-07-28 12:27 — Deep Research 最新调度规则文档同步

- 完成内容：
  - 更新 `docs/deep-research.md`，说明 `workflow` tool 始终只能从顶层会话启动，不跟随普通 `task` 嵌套深度配置。
  - 更新 `docs/project.md` 和 `docs/project-map.md`，补充 subagent 最大深度配置来源与 workflow 顶层边界。
  - 更新 `docs/codemap.md`，把最新上游调度边界补丁纳入 Deep Research / Workflow 完整移植 checklist。
- 验证结果：
  - `rg -n "WEEPCODE_SUBAGENTS_MAX_DEPTH|subagents_max_depth|MaxSubagentDepth|workflow.*顶层|top-level|仅顶层|\\[subagents\\]\\.max_depth" crates/codegen/weepcode-shell/src crates/codegen/weepcode-tools/src crates/codegen/weepcode-config-types/src docs/deep-research.md docs/project.md docs/project-map.md docs/codemap.md` 命中新配置与 workflow 顶层限制的代码/文档入口。
- 遇到的问题与下一步：
  - 无新增问题。
  - 下一步跑最终格式、diff 和生产编译/targeted test 门禁。

### 2026-07-28 11:54 — Subagent 深度配置链路验证

- 完成内容：
  - 修复 `spawn_session_actor` 内层参数漏透传，将 `subagents_max_depth` 从主会话传到 session actor rebuild spec。
  - 确认子 agent spawn 继承同一最大深度配置，并在 child tool policy 中按配置裁剪 `task` tool。
- 验证结果：
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check -p weepcode-shell --tests` 通过，用时 1m55s。
- 遇到的问题与下一步：
  - 首次 shell 检查报 `subagents_max_depth` 在内部 spawn helper 中未定义；已补透传。
  - 下一步跑格式化、pager/shell/bin 生产检查和 workflow 相关 targeted tests。

### 2026-07-28 11:47 — Deep Research 调度边界补齐

- 完成内容：
  - 拉取上级目录 `../grok-build` 最新源码，确认上游从 `b41c75a` 更新到 `02d9359`。
  - 对比最新上游 workflow 相关改动，确认 `weepcode-workflow` 引擎本体仅剩品牌/包名差异。
  - 移植最新上游能力侧补丁：`task` tool 支持 host 注入 `MaxSubagentDepth`，`workflow` tool 明确保持仅顶层会话可启动。
  - 接通 `[subagents].max_depth` / `WEEPCODE_SUBAGENTS_MAX_DEPTH` / remote `subagents_max_depth` 到 `Config`、`AgentRebuildSpec`、子 agent spawn 与 child tool policy；默认值仍为 1。
  - 补齐 `weepcode-tools` backend 测试 fixture 的 workflow/subagent 新字段。
- 验证结果：
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo test -p weepcode-tools raised_max_depth_allows_nested_spawn` 通过，1 个目标测试通过，0 warning。
- 遇到的问题与下一步：
  - 首次测试编译暴露 3 个 `SubagentRequest` 测试 fixture 漏字段，已补齐。
  - 下一步跑 shell 编译与 workflow 相关测试，确认配置链路和子 agent 调度签名无漏接。

### 2026-07-28 07:09 — 格式化与 whitespace 门禁

- 完成内容：
  - 执行 `cargo fmt --all` 格式化 deep-research 相关 Rust 改动。
  - 复查格式化和 diff whitespace。
- 验证结果：
  - `cargo fmt --all -- --check` 通过。
  - `git diff --check` 通过。
- 遇到的问题与下一步：
  - 首次 `cargo fmt --all -- --check` 仅发现 rustfmt 换行/缩进差异；已由 `cargo fmt --all` 修复。
  - 下一步跑 workflow 相关单元测试目标。

### 2026-07-28 06:34 — Deep Research 文档同步

- 完成内容：
  - 新增 `docs/deep-research.md`，说明 `/deep-research`、`/workflow`、`/workflows`、workflow 目录、运行时架构、持久化和开发入口。
  - 更新 `README.md`，补充 Deep Research 用户入口和文档链接。
  - 更新 `docs/project.md`，把 `weepcode-workflow` 纳入架构层级，并补充 Workflow / Deep Research 数据流。
  - 更新 `docs/project-map.md`，新增 Workflow 与 Deep Research 调度地图、持久化说明和常见改动入口。
  - 更新 `docs/codemap.md`，记录 Deep Research / Workflow 完整移植范围和门禁。
- 验证结果：
  - `rg -n "deep-research|workflow|Workflow|\\.weepcode/workflows|~/.weepcode/workflows|weepcode-workflow" README.md docs/deep-research.md docs/project.md docs/project-map.md docs/codemap.md` 命中所有新增入口。
  - `git diff --check` 通过。
- 遇到的问题与下一步：
  - 文档只描述已接入的 runtime/UI/持久化路径，不承诺未实现的跨进程继续运行语义。
  - 下一步跑 `cargo fmt --all -- --check`，必要时先格式化 Rust 改动再复跑。

### 2026-07-28 01:53 — Deep Research shell/bin 编译门禁

- 完成内容：
  - 在 pager 任务面板和状态输出补齐 workflow 接线后，复跑 shell 与最终二进制编译门禁。
  - 确认 `weepcode-workflow`、shell workflow runtime、tools workflow tool 与 pager workflow UI 能被 `weepcode-pager-bin` 依赖链完整编译。
- 验证结果：
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check -p weepcode-shell` 通过，用时 1m32s。
  - `PROTOC=$PWD/.tools/protoc/bin/protoc cargo check -p weepcode-pager-bin` 通过，用时 7m51s。
- 遇到的问题与下一步：
  - 并行启动两个 Cargo 检查时 `weepcode-pager-bin` 等待 build directory lock；检查结果不受影响。
  - 下一步同步 deep-research 架构/用户文档，之后跑 `cargo fmt --all -- --check` 与 `git diff --check`。
