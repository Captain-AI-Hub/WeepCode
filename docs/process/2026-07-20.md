# 2026-07-20 进度

当日目标：完成 Phase 7（死代码切除）—— 接手前序（"kimi"）半途而废的 7a 编辑，
把被改坏的编译修好，并按 codemap 推进 7a–7f。

## 起点状态（问题）

- 工作树**编译失败**：前序在 `weepcode-pager/src/app/effects/mod.rs` 删了
  `Effect::ShareSession` arm，并只删了一半 `Effect::SetCodingDataSharing` /
  `Effect::SendFeedback` arm（删了 pattern，留着 body）→ 解析错误
  `expected pattern, found =>`（行 3080 / 3162），后续错误被语法错误掩盖。
- 前序已删：`Action::{SendFeedback,SetCodingDataSharing,ShowPrivacyInfo}`、
  `Effect::SetCodingDataSharing`、`TaskResult::{CodingDataSharingUpdated,CodingDataSharingFailed}`、
  `PromptInputMode::Feedback` 等枚举变体；shell `extensions/{share,feedback,privacy}.rs` +
  mod 声明；但留下了大量引用这些已删类型的测试与孤立代码。

---

## Phase 7a（pager 侧）—— 修复编译 + 清死测试 ✅

- `effects/mod.rs`：删除两个孤立 arm body（privacy/coding-data、feedback）。
- 删除引用已删变体的死测试：
  - `dispatch/tests/router.rs` `send_feedback_clears_active_ephemeral_tip`
  - `dispatch/tests/settings.rs` 的 `"coding_data_sharing"` match 臂
  - `dispatch/tests/status.rs` 整段 show_privacy + coding_data_sharing 测试块（行 63–793）
  - `tests/settings_e2e.rs` `pr9_coding_data_sharing_picker_enter_dispatches_set_commit`、
    `pr9_privacy_slash_command_parses_aliases`
  - `agent_view/mod.rs` 内联测试 `is_exit_key_feedback_uses_stricter_set`
- 删除随之死亡的辅助：`dispatch/status.rs` 的 `push_system_to_any_agent`、
  孤立 import `refresh_open_settings_modals`、`tests/mod.rs` 的 `scrub_error_for_toast` import。
  （`scrub_error_for_toast` 本身保留——`task_result.rs` 仍在用，是通用 toast 清洗。）
- 验证：`cargo check -p weepcode-pager --tests` 通过，无新增警告。

**遗留（更深、更大，单独排期）**：
- shell 侧 feedback 子系统（`session/{feedback,feedback_manager}.rs`、
  `agent/feedback_client.rs`、`mvp_agent/agent_ops.rs` 内 feedback_credentials/manager、
  `extensions/notification.rs`、`session/slash_commands.rs` 的 `BuiltinAction::Feedback`）——
  深度织入 agent 核心，规模与 7c voice 相当。
- `weepcode share` CLI 子命令 + `share_cmd.rs` + shell `session::share` + `remote/client.rs::share_session`
  + `weepcode/share_session` ACP（web-share，xAI 服务）。
- `coding_data_sharing` setting + `coding_data_retention_opt_out` 字段（仍从 auth meta 加载，
  但 ShowPrivacyInfo/SetCodingDataSharing 已删 → 显示/修改链路已断；彻底删需动 AuthEntry 反序列化）。
- slash registry 对 share/feedback/imagine/imagine-video/privacy 的硬隐藏列表 +
  `set_share_visible`（命令实体已不存在，仅剩防御性 hide-by-name + 其测试）。

---

## Phase 7b —— 摘除 `weepcode-mixpanel` crate ✅

- `weepcode-telemetry/src/client.rs`：删 `use weepcode_mixpanel::Mixpanel`、
  `mixpanel` 字段 + Debug 行 + `from_config` 构造块 + `track()` 的 Mixpanel path +
  整个 `sync_profile()`（含 init/init_if_needed 两处调用）+ 其测试
  `sync_profile_is_noop_in_session_metrics_mode`；顺带清不再使用的 `Arc` import。
- `weepcode-telemetry/Cargo.toml`：删 `weepcode-mixpanel` 依赖。
- 根 `Cargo.toml`：删 workspace member 与 `[workspace.dependencies]` 两处。
- 删除 `crates/codegen/weepcode-mixpanel/` 目录。
- 验证：`cargo check -p weepcode-telemetry --tests` 通过。
- **遗留（Phase D，可选）**：`TelemetryConfig.{mixpanel_enabled,mixpanel_token}` 字段仍在
  （shell config 序列化仍写），telemetry 内已无人读 → 惰性死配置；彻底删需动 shell
  `config/mod.rs` 序列化与相关测试，单独排期。

---

## 死 CLI 子命令（share / setup / update 的一部分）✅

- `weepcode-pager/src/app/cli.rs`：删 `Command::Setup { json }`、`Command::Share(ShareArgs)` 变体
  （保留 `Sessions`、`Wrap` 等活命令）。
- `weepcode-pager-bin/src/main.rs`：删 `Command::Setup` / `Command::Share` 两个 arm。
- `weepcode-pager/src/lib.rs`：删 `pub mod share_cmd;`；删除 `src/share_cmd.rs`。
- `weepcode-shell/src/session/mod.rs`：删 `pub use self::share::{...}` 与 `pub mod share { ... }`。
- `weepcode-shell/src/remote/client.rs`：删 `share_session()`（无调用者）。
- **区分两种 share**：web-share（`session::share`、xAI 服务，死）已删；
  relay/leader 集群 share（`remote/client::share_url` + `relay::sync::build_share_url`）是**活**功能，保留未动。

---

## Phase 7f —— 摘除 `weepcode-update` crate ✅

- `weepcode-version/src/lib.rs`：新增 `pub const CHANNEL_LABEL: &str = ""`、
  `pub const CHANNEL_NAME: Option<&str> = None`（替代 `weepcode_update::channel_label/channel_name`）。
- 替换 5 处调用点（pager cli.rs、effects/mod.rs、views/welcome、main.rs ×3）为上述静态常量。
- `weepcode-pager/src/app/mod.rs` `run()` 与 `event_loop.rs`：去掉 `bg_update_rx` 参数、
  binding 与 select! arm（后台自动更新提示通道整体移除）。
- `main.rs`：删 `UpdateWaitHandle`/`bg_update_wait` 块、`finish_update_on_exit`、
  `build_update_config`/`should_check_for_updates`/`stdio_auto_update_enabled`/`is_managed_install`、
  3 处 `enforce_minimum_version_or_exit` 调用、startup `run_update_if_available` 块、
  stdio managed-install 块、leader `leader_auto_update` 构造（改 `None`）、`Update` 子命令 arm、
  相关测试与 `use weepcode_update::{...}`。
- `dispatch/status.rs`：删死函数 `commit_minimal_update_notice` + `dispatch/mod.rs` 其 import。
- 依赖：pager、pager-bin 的 `weepcode-update` 依赖、根 workspace member/dep 均删；
  删除 `crates/codegen/weepcode-update/` 目录。
- 清理 2 处指向已删 crate 的过时 doc 注释（`util/config/resolve/version.rs`、`leader/mod.rs`）。
- 验证：`cargo check --workspace` 通过；`git grep weepcode_update` 仅剩无关的
  `WeepcodeSessionUpdate`（ACP 会话更新流，非本 crate）。
- **保留**：`--no-auto-update` CLI flag（兼容 no-op）；shell 侧 `LeaderAutoUpdateConfig` 类型
  （活类型，传 `None`）；`util/config/resolve/version.rs` 的 channel/minimum_version 解析
  （读 `version.json`，独立于 update crate）。

---

## 下一步

剩余 Phase 7（均大而深，需用户确认优先级）：7c（voice，大）、7a-shell feedback 子系统、
7d（billing/upsell）、7e（xAI OAuth，高风险核心认证）。
