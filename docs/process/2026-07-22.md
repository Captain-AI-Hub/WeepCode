# 2026-07-22 进度

当日目标：完成 Phase 7 剩余项中的 **7a-shell**（feedback 子系统 + coding_data_sharing
彻底切除）与 **7d**（billing/upsell 子系统摘除）。7e（xAI OAuth 修剪）经用户确认
本次不做，单独排期。

## Phase 7a-shell —— feedback 子系统 + coding_data_sharing 切除 ✅

（主体由子代理完成，中途超时后由主会话验收收尾。）

### shell 侧（weepcode-shell）
- 删整文件：`session/feedback.rs`、`session/feedback_manager.rs`、
  `agent/feedback_client.rs`、`session/acp_session_tests/feedback_turn_lookup_tests.rs`。
- `agent/mvp_agent/agent_ops.rs`：去 feedback_credentials/feedback_manager 织入。
- `extensions/notification.rs`：去 feedback 相关部分。
- `session/slash_commands.rs`：删 `BuiltinAction::Feedback` 及路由。
- `coding_data_sharing` setting + `coding_data_retention_opt_out` 字段全链路删除
  （auth meta 加载、settings 定义、写入器、测试）。

### pager 侧（weepcode-pager）
- slash registry 对 share/feedback/imagine/imagine-video/privacy 的防御性
  hide-by-name 及 `set_share_visible` 已清除（hide 机制本身保留：
  dashboard/recap/auto/hooks/plugins 仍是活 gate）。
- `views/settings_modal/tests.rs` 的 2 个 `two_line_row_*` 测试修复：
  原合成 meta 用假 key（`test_two_line_row`），`current_value_for` 对未知 key
  返回 `None` → 行高恒 1，测试必败。改用真实注册表 + 生产 `render_mermaid` 行
  （label 23 格，width=28 必触发两行布局），断言同步更新。

### 验证
- `git grep coding_data_sharing|coding_data_retention_opt_out|set_share_visible|
  BuiltinAction::Feedback|feedback_manager|feedback_client`：
  weepcode-* crate 内仅剩 3 处注释引用（settings_modal/tests.rs ×1、
  settings_e2e.rs ×2，均标注 "retired in Phase 7a"）。
- `cargo check --workspace` / `--tests`：0 error / 0 warning。
- `cargo test -p weepcode-pager --lib two_line_row`：2 passed。
- `cargo test -p weepcode-pager --lib` 全量：7135 passed / 13 failed，其中：
  - 8 个 = 上游既有失败基线（scrollback teal ×6、picker、command_palette），不变
  - 3 个 = billing/dashboard `/imagine` 受限命令 upsell（**7d 范畴**，随后处理）
  - 2 个 = two_line_row（本轮已修，复跑后清零）

## Phase 7d-pager —— billing/upsell 子系统摘除（pager 侧）✅

（主体由子代理完成，中途超时留下坏树，由主会话验收收尾。）

### 删除清单（weepcode-pager）
- 整文件：`app/dispatch/billing.rs`、`app/dispatch/tests/billing.rs`、
  `views/credit_bar.rs`、`app/subscription.rs`、`slash/commands/usage.rs`、
  `scrollback/blocks/credit_limit.rs`。
- `Effect::FetchBilling`、`AppView` 的 `credit_balance`/`auto_topup`/`usage_visible`/
  `tier_restricted_commands`/`has_access`、`AgentSession::credit_limit_blocked`、
  `build_hints` 的 `selected_is_credit_limit` 形参，及全部 dispatch 臂/视图渲染/测试。
- 3 个既有失败测试（billing upsell ×2、dashboard upsell toast ×1）随子系统删除。
- 用户文档 `docs/user-guide/04-slash-commands.md` 删 `/usage` 条目。

### 主会话收尾（子代理超时后的坏树修复）
- `dispatch/tests/session/fork.rs`：fork 继承测试去 credit_balance/auto_topup/usage_visible 段。
- `dispatch/tests/session/lifecycle.rs`：SessionCreated effects 7→6（去 FetchBilling）；
  worktree 测试去 FetchBilling 断言。
- `acp_handler/tests/settings.rs`：tier 测试去 usage_visible/tier_restricted_commands 断言
  （保留 is_api_key_auth 翻转语义）。
- `acp_handler/tests/queue_and_adoption.rs`：删 `credit_limit_response_discards_adoption_buffer`
  （测的是已删的 credit-limit early return）。
- `acp_handler`：删随之死亡的 test-only shim `apply_session_event_for_test` 及 re-export。
- `views/agent.rs`：6 处 build_hints 测试调用点去掉已删的 `selected_is_credit_limit` 实参。
- `weepcode-pager-render/src/link_opener.rs`：删 `ensure_query_param`（SuperGrok upsell
  专用，仅剩自测）及 6 个单测——7d-shell 遗留项已清。
- weepcode-pager-minimal 的 `minimal_auth_hint` has_access 参数遗留由 7d-shell 子代理修复。

### 验证
- `cargo check -p weepcode-pager --tests`：0 error / 0 warning。
- `cargo test -p weepcode-pager --lib`：6960 passed / **8 failed = 上游既有基线**
  （scrollback teal ×6、picker、command_palette），billing/two_line_row 失败全部清零。

## Phase 7d-shell —— billing/订阅/付费门死代码摘除 ✅

pager 侧（dispatch/billing.rs、credit_bar、app/subscription.rs、/usage、Effect::FetchBilling 等）
此前已删，本轮摘除 shell 侧整条 billing 链路（已无调用方）。

### 删除清单（weepcode-shell，除注明外）
- billing ext：`extensions/billing.rs` 整文件删除；`extensions/mod.rs` 去 `pub mod billing;`；
  `agent/mvp_agent/acp_agent.rs` 删 `"weepcode/billing"` / `"weepcode/auto-topup-rule"` 两个
  ext_method 分发分支。`extensions/auth_gate.rs` 保留。
- check_subscription：`extensions/auth.rs` 删 `"weepcode/auth/check_subscription"` 注册与
  `handle_check_subscription`。
- 付费门主体（`agent/mvp_agent/`）：
  - mod.rs 删 `enforce_weepcode_code_access`、`retry_subscription_check`、
    `spawn_post_unblock_jwt_and_catalog_retry`、`PostUnblockJwtRetryInFlightGuard`、
    `settings_allow_access`、`jwt_claim_matches_user_subscription_tier`、
    `tier_allowed: Cell<bool>` 与 `post_unblock_jwt_retry_in_flight` 字段；
    `auth_response_with_meta` 去 gate 构造与 "subscription required" fallback（AuthMeta 不再有 gate）。
  - acp_agent.rs 删三处 `enforce_weepcode_code_access` 调用点（new_session / cached_token / OIDC）。
  - agent_ops.rs 删 `tier_allowed` / `post_unblock_jwt_retry_in_flight` 构造点。
  - tests.rs 删 settings_allow_access ×4、jwt claim match ×2、guard ×1、
    allow_access/on_demand_enabled 解析 ×2；`polled_announcements_apply_touches_announcements_only`
    中的 `allow_access` 断言一并移除。
- subscription_check：`agent/subscription_check.rs` 整文件删除 + `agent/mod.rs` 声明；
  `auth/model.rs` 删 `UserInfo.subscription_tier` 字段及 4 个单测；
  `auth/manager.rs` refresh_chain 注释与 `manager_tests.rs` "pre-subscription-jwt" 测试
  文案中性化（通用 refresh_chain 逻辑保留，测试改名 `server-rejected-jwt`）。
- RemoteSettings 死字段（weepcode-config-types/src/lib.rs）：删
  `subscription_watch_interval_secs`、`subscription_tier`（保留 `subscription_tier_display`）、
  `gate_message/gate_url/gate_label`、`allow_access`、`on_demand_enabled`、
  `usage_billing_redirect_url`；同步删 mvp_agent/mod.rs `SettingsUpdateNotification` 对应字段
  与 emit 处；`auth/meta.rs` 删 `GateInfo` 结构与 `AuthMeta.gate` 字段、`auth/mod.rs` 去 re-export。
- pager PTY 死测试：删 `weepcode-pager/tests/pty_e2e/subscription_watch_and_gate_verify_pty.rs`
  及 `pty_e2e_config_ui.rs` 的 mod 注册。

### 顺手修复（pager 侧 7d 遗留的编译断点）
pager 侧删 `AppView::has_access` 后，weepcode-pager-minimal 仍有两处调用
（live.rs / overlay.rs）导致 `cargo check -p weepcode-pager-bin` 失败。已将
`minimal_auth_hint` 的 `has_access: bool` 参数摘除（ZDR gate 保留），调用点与
auth.rs 内 4 个单测同步更新。

### 验证
- `cargo check -p weepcode-shell --tests`：0 error / 0 warning。
- `cargo check -p weepcode-pager-bin -p weepcode-config-types --tests`：0 error / 0 warning。
- `cargo test -p weepcode-shell`：5554 passed / 4 failed / 13 ignored。4 个失败均为既有
  （非本次引入，所在文件 agent/app.rs、leader/server.rs、claude_import.rs 本轮未触碰）：
  - `internal_reload_request_line_uses_wire_ext_prefix`、`machine_wide_broadcast_classifier_
    matches_both_wire_forms`、`models_update_broadcasts_to_all_clients`：断言仍期望 `_x.ai/*`
    线前缀，代码已产出 `_weepcode/*`（Phase 5 改名遗留）；
  - `gate_load_claude_env_returns_empty_when_marker_set`：env marker 相关。
  docs/process 无更早 weepcode-shell 测试基线可比，记为新基线。
- `cargo test -p weepcode-pager-minimal --lib`：64 passed / 0 failed。
- `git grep` 残留检查：`weepcode/billing`、`auto-topup-rule`、`check_subscription`、
  `subscription_check`、`tier_allowed`、`GateInfo`、`on_demand_enabled`、
  `usage_billing_redirect_url`、`gate_message/gate_url/gate_label`、
  `subscription_watch_interval` 在 crates/ 下零残留（仅 link_opener.rs:294-296 一行
  注释提及 gate_url，见遗留）。`allow_access` 仅剩 mock server/PTY 测试的 settings
  JSON payload（RemoteSettings 无 deny_unknown_fields，反序列化安全忽略，属惰性字段）。
- clippy（weepcode-shell/-pager-minimal/-config-types）：仅有既有警告
  （run_loop.rs / leader/server.rs / relay.rs 的 else-if 风格，本轮未触碰文件）。
- fmt：workspace 存在大面积既有 fmt 漂移，未跑 `cargo fmt` 以免越界改动；
  本轮新增/改动行已手动对齐 rustfmt 风格。

### 遗留问题
- ~~`weepcode-pager-render/src/link_opener.rs` 的 `ensure_query_param`~~：已由主会话删除
  （连 6 个单测），零残留。
- mock_server `preset_allow_access` 与各 PTY 测试 settings JSON 里的 `"allow_access": true`
  已成为惰性字段，可随后续 mock 清理一并移除。
- 上述 4 个既有测试失败待改名收尾阶段处理。

## Phase 7e —— xAI 专属 OAuth 修剪 ✅

### 删除清单（weepcode-shell，除注明外）
- **devbox 链路整根拔除**（stub 本就恒 false，全链路死代码）：
  - `auth/devbox_login_stub.rs` 整文件删除 + `auth/mod.rs` 的 `#[path]` mod 声明。
  - `auth/flow.rs`：`run_auth_flow_inner` devbox 自动迁移块、`mint_session_noninteractive`
    devbox 分支（连带删除仅剩该分支使用的 `persist_or_use_minted` 及其单测）、
    `run_cli_login` 的 `devbox: bool` 参数与 `--devbox` 分支。
  - `auth/manager.rs`：`is_devbox_environment`/`set_devbox_env_for_test`/`devbox_override`
    字段、`try_devbox_recovery`、`auth()` 两处 devbox 兜底块；连带删除因此失去全部
    调用方的 `save_without_enrichment`。
  - `auth/recovery.rs`：`RecoveryStep::DevboxRecovery` 变体与状态机臂；`Done` 臂改为
    优先透出 `authority_error`（承接原 DevboxRecovery 臂语义， transient 轴不变）。
  - `agent/app.rs`：`migrate_devbox_auth_if_legacy` 及 leader 启动调用点。
  - `agent/mvp_agent/acp_agent.rs`：cached_token 认证的 devbox legacy 迁移块
    （WebLogin 拒绝路径保留）。
  - `session/acp_session_impl/sampler_turn.rs`：sampler 401 devbox re-mint 块
    （通用 `try_recover_unauthorized` 路径保留）。
  - `trace_classifier/mod.rs`：`skip_if_devbox`/`DEVBOX_SA_TOKEN_PATH` 守卫及 4 处调用
    （守卫理由——AuthManager 会 devbox mint——已不存在）。
  - pager 侧：`app/cli.rs` AddModel 的 `#[arg(skip)] devbox` 字段 + pager-bin main.rs
    对应解构。
- **xAI 默认 OAuth2 配置**（`auth/config.rs`）：
  - `Default::default()` 不再合成指向 `https://auth.x.ai` 的默认 oauth2
    （含 obfstr 硬编码 client_id `b1a00492-…`）；`oauth2` 仅剩
    `WEEPCODE_OAUTH2_ISSUER`/`WEEPCODE_OAUTH2_CLIENT_ID` env 或 TOML 显式配置。
  - 删 `weepcode_oauth2_issuer()`、`use_local_auth()`（`WEEPCODE_LOCAL_AUTH`）、
    `WEEPCODE_OAUTH2_LOCAL_ISSUER`（localhost:22255 本地 accounts-app）。
  - **accounts.x.ai CORS**：删 `PROD_ACCOUNTS_APP_ORIGINS`/
    `allowed_accounts_app_origins()`/`accounts_app_cors_layer()`；
    `oidc/login.rs` loopback 回调服务器不再挂 CORS 层（授权码走顶层浏览器重定向，
    无需 CORS），固定回调端口 56121 的 local-dev 分支一并移除。
  - `auth_scope()` 的 `unreachable!` 改为稳定占位 scope
    `weepcode::oauth2-unconfigured`（BYOK 部署下 `AuthManager::new` 必走此路径，
    不能 panic；api_key 存 `weepcode::api_key` scope，不受影响）。
  - 删两个冻结 xAI 服务端契约的测试（`allowed_accounts_app_origins_are_frozen`、
    `default_oauth2_scopes_are_frozen`）；`manager_tests` 的
    `auth_scope_uses_oauth2_when_present` 改显式配置并新增 fallback 测试；
    `flow.rs` 新增 `test_oauth2_provider`/`cfg_with_first_party_oauth2` 测试辅助，
    4 处依赖默认 oauth2 的测试改显式构造。
  - weepcode-shell/Cargo.toml 移除随之无引用的 `tower-http`（cors）依赖。

### 保留清单（含理由）
- `WEEPCODE_COM_METHOD_ID = "grok.com"` 及 `weepcode_com_auth_method`：ACP 线协议
  method id，pager `acp/mod.rs`、`acp_agent.rs:704`、`agent_ops.rs:731` 深耦合；
  pinned OIDC 路径仍在用。硬删牵一发动全身，按任务约定跳过。
- `WEEPCODE_OAUTH2_ISSUER` + `is_weepcode_oauth2_issuer` + `is_weepcode_auth`：
  活的一方会话分类器（agent_ops 模型可见性/遥测/relay 十余处调用点），是「比较
  字面量」而非「默认值」；改语义属大型行为变更，不在修剪范围。
- `LEGACY_AUTH_SCOPE` / `LEGACY_SCOPE`（accounts.x.ai/sign-in）：纯本地 auth.json
  存储键，用于陈旧 legacy 条目清理与读取回退，零网络流量。
- 通用 OIDC 流（`oidc/` 全部、`OidcAuthConfig`/`OAuth2ProviderConfig::from_env`、
  TOML 显式 oauth2）、外部 auth provider 命令、device_code 流（端点由配置的
  issuer 派生，无硬编码 xAI）、api_key 认证：全部保留。
- relay/leader 的 grok.com WebSocket 域名常量（G5 白名单）未动。

### 验证
- `cargo check -p weepcode-shell -p weepcode-pager --tests`：0 error / 0 warning。
- `cargo test -p weepcode-shell --lib`：5552 passed / **4 failed = 既有基线**
  （3× `_x.ai` 线前缀断言 + 1× claude_import env marker，与 7d 记录一致，无新增）；
  通过数 5554→5552 对应本轮净删 2 个测试（-3 死代码测试 +1 fallback 测试）。
- `cargo test -p weepcode-pager --lib cli`：316 passed / 0 failed。
- `git grep devbox_login|mint_devbox_auth|is_devbox_environment|try_devbox_recovery|
  set_devbox_env_for_test|devbox_override|run_devbox_login|DevboxRecovery|
  weepcode_oauth2_issuer|use_local_auth|WEEPCODE_LOCAL_AUTH|WEEPCODE_OAUTH2_LOCAL_ISSUER|
  accounts_app_cors_layer|allowed_accounts_app_origins|PROD_ACCOUNTS_APP_ORIGINS|
  skip_if_devbox|persist_or_use_minted`：crates/ 下零残留（仅保留的
  `is_weepcode_oauth2_issuer` 命中）。
- `docs/project.md` 耦合点清单对应行已同步。

### 遗留问题
- 存量 auth.json 中 xAI 默认 scope（`https://auth.x.ai::b1a00492-…`）的会话条目
  不再被读取（fallback scope 变更的必然结果）；legacy WebLogin scope 回退读取
  保留，不影响 BYOK。
- pinned `preferred_method = "oidc"` 且未配置任何 OIDC/OAuth2 provider 时，交互
  登录会以 "No OAuth2 configuration available" 失败——原默认 xAI 后端的语义化
  降级，属预期。
- `activity.rs`/`leader/server.rs`/`extensions/worktree.rs` 等注释中的 "devbox"
  字样指集群/远程部署形态（非 devbox 登录 helper），未动。

## 待办
- ~~Phase 7e（xAI OAuth 修剪）：经用户确认本次不做，单独排期。~~ 已完成（见上）。
- announcements promo CTA（welcome/dashboard 的 upgrade CTA、`promo_cta`、`hit_upgrade_cta`）：
  数据源是 xAI announcements（Provider 模式永不拉取），属惰性死 UI，但 Phase 3 决策保留
  announcements 子系统整体；是否连根拔需用户拍板，建议并入 7e 或单列阶段。
- 7d 明确保留的决策点：`subscription_tier_display`/`AuthMeta.subscription_tier`
  （is_api_key_auth 推导契约）、Imagine `tier_restricted` 链路（tier.rs + ImageGen/VideoGen）、
  telemetry 的 `resolve_subscription_tier_for_telemetry`、`sampling/error.rs` 的
  free-usage/SuperGrok upsell 文案、`SuppressReason::CreditBlock`（BYOK 通用）。
