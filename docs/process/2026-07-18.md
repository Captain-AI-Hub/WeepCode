# 2026-07-18 进度

当日目标：完成项目审计与文档奠基（Phase 0），启动 Phase 1（去除强制登录）。

---

## [12:00] Phase 0 — 文档奠基 ✅（硬门禁 G0 通过）

- 完成内容：
  - 派出 4 个探索代理完成深度审计：认证/强制登录链路、配置持久化、TUI 架构与品牌面、API 请求与模型层
  - 关键审计结论：
    - 三种 API 格式（OpenAI Responses / Chat Completions / Anthropic Messages）的协议层**已存在**于
      `xai-grok-sampler` + `ApiBackend`（xai-grok-sampling-types/src/types.rs:1013），改造重点是认证门与 UX，不是协议
    - 强制登录链路：`agent/auth_method.rs:139` 通告方法 → pager `acp/mod.rs:574` 算 `needs_login` →
      `event_loop.rs:635` 播种登录态 → `app_view.rs:1103` 阻断会话
    - per-model BYOK（`[model.*]` 配 api_key）今天即可绕过 xAI 登录，但被 TUI 入口逻辑挡住
    - 缺口：Messages 后端不自动注入 `anthropic-version` 头；所有请求带 `x-grok-*` 追踪头
  - 写入 `docs/project.md`（架构）、`docs/rule.md`（规则）、`AGENTS.md`、`docs/codemap.md`（计划+硬门禁）
  - 用户确认：产品名 **WeepCode**；去 grok 深度为**功能优先**（crate/env/配置目录本期不动，全量改名列入 Phase 5）
- 验证结果：G0 — 四文档存在且互相引用一致 ✅
- 下一步：Phase 1 — 去除强制登录（shell 侧 auth_method + pager 侧启动门 + 欢迎屏入口改造）

---

## [17:30] Phase 1+2 实现（代码完成，待编译验证）

- 说明：中途派出的 coder 子代理超时后被用户叫停，此后全部由主会话直接实现；子代理已完成的
  shell auth 部分经逐行 diff 审查后保留。期间 `/Volumes/SU710` 掉盘一次，恢复后继续。
- 完成内容：
  - shell `agent/auth_method.rs`（子代理遗留，已审查）：unpinned 路径移除 grok.com/oidc 通告，
    新增 `provider.setup` 方法（`AuthMethodKind::ProviderSetup`，interactive 但非 session-based），
    cached_token 回退目标改为 provider.setup；单测同步更新
  - shell `agent/mvp_agent/acp_agent.rs`：OIDC 通告限定 pinned 路径；`authenticate(provider.setup)`
    防御分支返回可读指引；扩展路由挂入 `weepcode/provider/*`
  - shell 新增 `util/config/provider_profile.rs`：三格式映射（responses/chat_completions/messages），
    `[model.<slug>]` upsert + `[models].default` 落盘、原子写、0600 权限、slug 冲突加序号、
    拒绝覆盖不可解析配置；8 个单元测试
  - shell 新增 `extensions/provider_setup.rs`：`weepcode/provider/save` 处理器（校验→落盘→
    复用提取出的 `session_admin::reload_models_from_disk` 热更新模型目录）
  - sampler `client.rs`：Messages 后端自动注入 `anthropic-version: 2023-06-01`（extra_headers 优先）；
    `is_first_party_inference_base_url` 谓词，`x-grok-*` 追踪头（默认头 4 个 + 每请求 6 处调用点）
    仅发往 x.ai/grok.com 域；6 个新单元测试
  - pager 新增 `views/provider_setup.rs`：五字段表单（格式选择/base_url/api_key 掩码/模型 id/展示名），
    Tab/方向键导航、Enter 校验提交、Esc 取消、粘贴支持、格式切换联动 URL 预设；4 个单元测试
  - pager 接线：Action::ProviderSetupSubmit/Cancel、Effect::SaveProviderProfile、
    TaskResult::ProviderProfileSaved、`dispatch_login` 识别 provider.setup 开表单、
    欢迎屏输入拦截与 Pending 渲染分支、保存成功→authenticate(xai.api_key) 链路
  - pager `headless.rs`：无凭证报错文案改为 Provider 配置指引
  - pager-bin `main.rs`：`grok login` 子命令改为打印 Provider 配置指引（不再跑 xAI OAuth）
- 验证结果：单元测试已写但**尚未编译**——crates.io API 域名被 403（网络环境限制），
  index/static 直连可用，`cargo fetch` 全量下载进行中
- 下一步：fetch 完成后 `cargo check` 三个 crate 修错，跑 G1/G2 门禁

---

## [19:45] Phase 1+2 完成（硬门禁 G1/G2 通过）✅

- 环境注记：crates.io API 域名 403（区域限制）但 index/static 直连可用，`cargo fetch` 全量
  下载完成后 `--offline` 编译可行；构建需要 protoc，已在 `.tools/protoc/`（gitignored）放置
  本地 protoc 29.3，构建时 `export PROTOC=$PWD/.tools/protoc/bin/protoc`。
- 修复的问题：
  - `upsert_provider_profile_at` 未内部校验导致带尾斜杠的 base_url 落盘（测试抓住）→ 持久化边界内化校验
  - pager clippy `new_without_default`（ProviderSetupForm 补 Default impl）、router 多余 import
  - PTY 冒烟脚本两轮修正：pty 0x0 尺寸导致 TUI 不渲染；ANSI 转义吃掉断言字符串中的空格
- 验证结果（硬门禁）：
  - G1-1 `cargo check -p xai-grok-{sampler,shell,pager,pager-bin}` 通过（1m48s）
  - G1-2 PTY 冒烟（`.tools/provider_setup_smoke.py`）：干净 HOME 启动直接显示
    "Configure API Provider" 表单，无 grok.com/auth.x.ai 登录 UI ✅
  - G1-3 干净 HOME 启动 12s 内 `lsof` 无任何 TCP 连接（含子进程）✅（已有机制：
    无凭证时 `resolve_prefetch_env_from_parts` 跳过远端预取；遥测默认 Disabled）
  - G1-4 `cargo test -p xai-grok-shell auth`：467 passed / 0 failed ✅
  - G2-1 测试：sampler 160、shell provider_profile 8、pager provider_setup 4 全过 ✅
  - G2-2 PTY 端到端：表单依次填入 openai-compatible + `http://127.0.0.1:9/v1` + key + 模型 id +
    展示名 → `~/.grok/config.toml` 正确写入 `[model.smoke-provider]` 全字段 +
    `[models].default = "smoke-provider"`，权限 0600；重启后不再出现设置表单（BYOK eager auth）✅
  - G2-3 三格式映射由单测覆盖（responses/chat_completions/messages + x_api_key +
    anthropic-version）；真实供应商联机调用因无可用 key 未验证，如实记录
  - G2-4 clippy：改动文件无新增警告（shell 存量 26 个 leader/relay 警告与本次无关）✅
- 结论：Phase 1（去强制登录）与 Phase 2（Provider 设置+持久化）完成，进入 Phase 3。

---

## [21:30] Phase 3 — 切断 xAI 服务耦合（mock 端到端抓住两个真问题）

- 验证方法：本地 OpenAI 兼容 mock（`.tools/provider_mock.py`，127.0.0.1:18321）+
  手写 BYOK 配置跑 headless `-p "say hi"`，抓 mock 端全部请求头与 body。
- 发现并修复：
  1. **xAI 专用头泄漏**：`X-XAI-Token-Auth`/`x-authenticateresponse`/`x-grok-client-mode`
     被 `inject_url_derived_headers` 发往第三方端点——因为 `is_cli_chat_proxy_url` 对
     loopback 主机永远放行（xAI 本地开发用）。修复：新增 `is_first_party_proxy_url`
     排除 loopback（agent/config.rs），补单测（三种 loopback 形态）。
  2. **辅助模型 id 泄漏**：session 标题查询在 aux 模型（默认 grok-build）解析不到凭证时，
     fallback 克隆主配置但把 model id 改写成 grok-build → 第三方端点必然 404。
     修复：`build_summary_client` 的 None 分支保留主模型 id（agent_ops.rs），加 warn 日志。
- 确认无需改动（既有机制已满足）：
  - 模型目录拉取：无凭证时 `EndpointAuth::Session` 分支在发请求前就 bail（remote/client.rs:739），
    启动期零网络（lsof 实测：干净 HOME 与 BYOK 重启后均 0 TCP 连接）
  - 内置 grok-build：`supported_in_api: false` 对 BYOK 用户不可见、不可选（visible_for_auth）；
    条目本身惰性，全量清理留 Phase 5
  - 遥测：无 xAI session 时 `TelemetryMode::Disabled`（config.rs:2080）；sentry 无 DSN 即 no-op
  - 付费门：`enforce_grok_code_access` 非 xAI 直通（mod.rs:1768），`is_xai_auth` 对 ApiKey 恒 false
    （auth/model.rs:134，已有 is_xai_auth_matrix 测试覆盖）
  - 欢迎屏 SuperGrok 门：`has_access() = gate.is_none()`，BYOK 无 gate 永不渲染
  - 公告/插件市场/voice：公告来自远端设置（无 session 无拉取）；市场源纯配置驱动；
    voice 需用户主动触发且无 xAI bearer 即失败——均无自动流量
- 改动：自更新 `should_check_for_updates` 硬禁用（pager-bin main.rs，xAI 更新通道对 WeepCode 无意义）
- 复测结果：mock 端 0 个 xAI 头；请求模型 id 全部 smoke-model-1（主请求+标题查询）；
  `mock-reply` 正常返回；`cargo test -p xai-grok-shell inject_url_derived` 5 passed
- 待完成：`cargo check --workspace`（后台运行中）→ G3 门禁收尾

---

## [21:50] Phase 3 完成（硬门禁 G3 通过）✅

- G3-1 mock 端到端：Provider-only 会话全部流量仅到本地 mock，零 xAI 域请求（请求头逐个核验）✅
- G3-2 `cargo check --workspace --offline` 通过（3m08s）✅
- G3-3 本文件已记录验证方式与结果 ✅
- 下一步：Phase 4 — 用户可见品牌替换为 WeepCode（clap/二进制名、欢迎屏、系统提示词、窗口标题、退出提示、README）

---

## [23:20] Phase 4 完成（硬门禁 G4 通过）✅

- 完成内容：
  - 标识：clap `name = "weepcode"`；bin target 改名（产物 `target/debug/weepcode`）；
    `--version` 输出 `weepcode 0.2.102`；`parse_and_apply_cwd` argv0 默认 weepcode；
    `--help` Usage 行与 PROMPT 示例同步
  - 界面文案：欢迎屏徽标/副标题/信任警告/登录标签、项目目录询问、设置项主题显示名、
    `/docs`（移除 web 外链）、`/usage`（移除 manage 外链）、窗口标题（含默认/reset）、
    macOS/Linux 睡眠抑制 reason、`grok --resume` → `weepcode --resume`（含测试）
  - 系统提示词：`DEFAULT_SYSTEM_PROMPT_LABEL = "WeepCode"`；templates 三份 md 去掉
    "released by xAI" 与 Grok Build 自述；`~/.grok/docs` 引用改 WeepCode TUI
  - 文档：根 README.md 重写；user-guide 全量 `grok` 命令→`weepcode`、Grok Build→WeepCode；
    02-authentication.md 重写为 Provider 认证模型
  - npm 包：package.json 改名 weepcode、标 private、去掉 xAI 平台依赖（发布管线留后续）
  - 修复测试 4 个（cli 名称/帮助头、billing 文案期望）；title.rs 全量断言同步
- 重要甄别：pager 全量测试初报 12 失败，其中 8 个（scrollback teal 渲染 6 + picker/palette
  光标 2）经 git worktree 基线（pristine HEAD 98c3b24）对照证实为**上游既有失败**，
  与本次改动无关（涉及文件均不在 diff 中）；另 4 个为品牌文案期望，已修
- 验证结果（硬门禁）：
  - G4-1 用户可见字符串：pager src 与 agent templates 中 Grok/xAI 字样清零
    （残留命中均为测试夹具、ACP 线协议方法名 `x.ai/*`、内部标识符——归 Phase 5）✅
  - G4-2 `weepcode --version` / `--help` 显示新名称；PTY 冒烟屏幕只见 WeepCode ✅
  - G4-3 `cargo check --workspace` 通过；pager lib 7258 passed / 8 failed（全部为上游既有失败，
    基线已证）✅；PTY 冒烟（G1/G2 流程）在新二进制上复过 ✅
- 结论：Phase 0–4 全部完成。WeepCode 现为去 xAI 化的通用 TUI：无强制登录、三格式
  Provider 设置+持久化、零 xAI 自动流量、用户可见品牌统一。Phase 5（crate/env/目录
  全量改名）未排期，见 codemap。

---
