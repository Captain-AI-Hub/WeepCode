# 2026-07-21 进度

当日目标：完成 Phase 7c——摘除 `weepcode-voice` crate 及全部 pager 语音 UI +
shell 侧 voice 会话支持（voice 在 WeepCode 里早已硬关：shell 的
`resolve_voice_mode()` 恒返回 `false`，无 WeepCode STT 后端，整条 pager 语音
管线是不可达死代码）。

## Phase 7c —— voice 全量摘除 ✅

### crate 级
- 删 `crates/codegen/weepcode-voice/` 整目录；root `Cargo.toml` 去成员；
  `weepcode-pager/Cargo.toml` 去 `weepcode-voice` 依赖。
- Cargo.lock 由 `cargo check --workspace` 自动更新。

### pager 侧（weepcode-pager）
- **删整文件**：`src/voice/{mod,auth,handle}.rs`、`src/app/dispatch/voice.rs`、
  `src/slash/commands/voice.rs`、`src/app/dispatch/tests/voice.rs`，及对应 mod 声明。
- **actions.rs**：删 5 个 Action 变体（`EnableVoiceMode`/`VoiceToggle`/`VoiceStop`/
  `SetVoiceCaptureMode`/`SetVoiceSttLanguage`）；actions/mod.rs 删 2 个 ActionId
  变体；actions/defaults.rs 删对应 ActionDef + Ctrl+Space/F8 绑定。
- **app_view.rs**：删 `VoiceState`/`VoiceTarget` 枚举 + `impl VoiceState`；6 个
  voice 字段（`voice_mode_enabled`/`voice_ui_active`/`voice_config`/`voice_auth`/
  `voice_cmd_tx`/`voice_state`）；整个 voice_* 方法簇（listening/recording_target/
  interim/begin_recording/esc_outcome/enforce_voice_session_bound/…）；2 处 esc 调用点；
  draw 内 voice 局部与 `agent.draw`/`prompt.draw` 尾部 voice 实参。
- **event_loop.rs**：删 voice_mode_enabled 解析块、VoiceConfig 装配、voice_stt_language
  override、voice_rx/voice_auth_factory 声明、ColdStart spawn 块、select! voice 臂、
  voice chord 拦截块、`voice_chord_action`/`is_voice_chord` 函数及测试。
- **dispatch/{router,prompt,dashboard,session/{fork,lifecycle,load},settings/{setters,ui}}**：
  删所有 voice 分发臂、`voice_stop_on_submit` 调用、`set_voice_mode_available`、
  voice_stt_language snapshot 字段、setters `set_voice_{capture_mode,stt_language}`。
- **acp_handler**：删 `PagerSettingsUpdate.voice_mode_enabled` 字段 + settings-update
  里的 voice 块；session_notification 的 `set_voice_mode_available`。
- **视图层**：prompt_widget `VoicePromptOverlay`/`wrap_voice_interim`/voice 渲染块；
  dashboard `voice_listening`/`voice_interim` 字段、`paint_record_badge`、voice_overlay；
  agent_view `hit_voice_stop_button`、recording 行绘制、`record_dot_pulse`、draw 签名
  去 3 个 voice 形参（16→13）；welcome/shortcuts_help 的 voice 引用。
- **settings**：defs.rs 删 `VOICE_CAPTURE_MODE_CHOICES`/`VOICE_STT_LANGUAGE_CHOICES`
  常量 + 2 个 SettingMeta；registry.rs 删 `canonical_voice_*`/`current_value_for`
  voice 臂及测试；modal state.rs 的 `setting_row_visible` 从 4 参（含 voice_mode/
  kitty_releases）精简到 2 参（meta, minimal），`enum_choice_gated_off` 去 voice 项
  + `kitty_releases` 形参。
- **死代码连带清理**：`client_identity.rs::client_user_agent`（仅 voice STT 用）、
  `weepcode-pager-render/glyphs.rs::record_dot`、event_loop `KeyEvent` 未用 import。

### shell 侧（weepcode-shell / weepcode-shared / weepcode-config-types）
- `agent/config.rs`：删 `Features.voice_mode`/`Requirements.voice_mode` 字段、
  `resolve_voice_mode`/`is_voice_mode_enabled` 方法。
- `config/mod.rs`：删 `pin_feature!(voice_mode)`。
- `mvp_agent/acp_agent.rs`：删 initialize meta 的 `"voiceMode"`。
- `weepcode-config-types`：删 `RemoteSettings.voice_mode_enabled`。
- `weepcode-shared/ui_config.rs`：删 `voice_capture_mode`/`voice_stt_language` 字段。
- `util/config/settings_writes.rs`：删 `set_voice_capture_mode`/`set_voice_stt_language`。
- `config/tests.rs`：删 `apply_requirements_pins_voice_mode_false`。

### 测试清理
全部引用已删 voice API 的测试整段删除（dispatch dashboard/lifecycle voice 测试、
agent_view voice_stop/voice_recording_overlay 测试、acp_handler voice_settings 测试、
app_view voice tier/Esc-voice 测试、prompt_widget wrap_voice_interim 测试、
peek 录音徽章测试、settings registry/modal voice 测试、settings_e2e voice 段）；
保留的非 voice 测试按新 draw/compute 签名调整实参。

## 验证

- `cargo check --workspace`：**0 error**（硬门禁 G7 通过）。
- `cargo check --workspace --tests`：**0 error / 0 warning**。
- `cargo test -p weepcode-pager --lib`：**7143 passed / 6 failed**。
  6 个失败全部属 **Phase 7a/7d** 范畴，与 voice 无关：
  - 3× billing/dashboard `/imagine` 受限命令（imagine 已在 7a 硬隐藏 → 7a/7d）
  - 1× `rows_contain_categories_and_settings_through_pr_14` 期望 Privacy 类别
    （7a 删 privacy 设置 → Privacy 类别空）
  - 2× `two_line_row_*` 用 `coding_data_sharing`（7a 待删字段）
- `cargo test -p weepcode-pager-minimal`：64 passed / 0 failed。
- `cargo test -p weepcode-pager --lib settings::`：142 passed（含
  `every_setting_has_dispatch_arm`，证明删 voice 设置未破坏分发覆盖）。

## 注意 / 待办

- **stash 陷阱**：中途用 `git stash push -u` 想对比 baseline，pop 时把已删的
  voice 文件作为新增（`A`）恢复了——Cargo.toml/mod 声明仍在，故未破坏编译，
  但磁盘上留下孤立 voice 文件；已二次 `rm` 清理。教训：大量重命名+删除的
  工作树慎用 `git stash -u` 对比 baseline。
- Phase 7c 已完成。剩余 6 个测试失败移交 **7a**（imagine/privacy/coding_data_sharing
  决策）与 **7d**（billing/upsell）。
