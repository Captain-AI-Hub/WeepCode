[package]
license = "Apache-2.0"
name = "weepcode-config-types"
version = "0.1.0"
edition.workspace = true
description = "Leaf configuration value types for the weepcode CLI, extracted from weepcode-shell for dependency inversion."

[dependencies]
agent-client-protocol = { workspace = true }
indexmap = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
strum = { workspace = true }
tracing = { workspace = true }
weepcode-config = { workspace = true }
weepcode-mcp = { workspace = true }

[features]
default-bazel = []

[lints]
workspace = true
