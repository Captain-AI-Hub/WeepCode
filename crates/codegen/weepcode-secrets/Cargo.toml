[package]
license = "Apache-2.0"
edition.workspace = true
name = "weepcode-secrets"
version = "0.1.0"
description = "Regex sanitizer for WeepCode outbound data and error-report scrubbing"

[dependencies]
regex = { workspace = true }
serde_json = { workspace = true }
url = { workspace = true }

[lints]
workspace = true
